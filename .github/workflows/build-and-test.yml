name: Continuous Integration
on:
  push:
  pull_request:
  schedule:
    # Run every day at 6:14PM
    - cron: '14 18 * * *'
jobs:
  build:
    name: HHVM ${{matrix.hhvm}} - ${{matrix.os}}
    strategy:
      # Run tests on all OS's and HHVM versions, even if one fails
      fail-fast: false
      matrix:
        os: [ ubuntu ]
        hhvm:
          - '4.73'
          - latest
          - nightly
    runs-on: ${{matrix.os}}-latest
    steps:
      - uses: actions/checkout@v2
      - name: Create branch for version alias
        run: git checkout -b CI_current_pull_request
      - uses: hhvm/actions/hack-lint-test@master
        with:
          hhvm: "${{matrix.hhvm}} -hhvm.hack.lang.enable_first_class_function_pointers=1"